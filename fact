#include<stdio.h>
int fact(int n);
int main()
{
    int num,c;
    printf("enter the number");
    scanf("%d",&num);
    c=fact(num);
    printf("the fact of the given number=%d",c);
    return 0;
}
int fact(int n)
{
    if(n==0)
    return 1;
    else
    return((n)*fact(n-1));
}
